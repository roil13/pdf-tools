## What this changes

<!-- And why. If it fixes an issue, link it. -->

## How you verified it

<!-- Concretely. "npm run verify passes" is the floor, not the answer:
     say what you actually checked and how you knew it worked. If you
     changed a PDF operation, say whether you read the output back with
     native qpdf and what survived. -->

- [ ] `npm run verify` exits 0
- [ ] Both `en` and `he` strings added, if any user-visible text changed
- [ ] Checked on Android (`npm run probe:android`), if it touches the platform layer

## Anything unfinished or unverified

<!-- Say so plainly. This is more useful than a confident summary. -->
